---
import { getLangFromUrl } from "@/i18n/utils";
import Dock from "@/components/ui/nav/Dock";

const lang = getLangFromUrl(Astro.url);
---

<div class="z-[999] my-2 h-10">
  <Dock client:load lang={lang} />
</div>
